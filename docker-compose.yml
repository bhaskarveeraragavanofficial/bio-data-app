version: '3.8'

services:

  web:
    build: .
    ports:
      - "5010:5010"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: biodata
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"